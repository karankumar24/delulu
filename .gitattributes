# LF for the shipped bundles on every checkout, so shipped.test.ts compares the same bytes everywhere.
* text=auto
plugin/hook/*.mjs text eol=lf

# Built by `npm run build` from app/src.
plugin/hook/handoff.mjs linguist-generated
plugin/hook/resume.mjs linguist-generated
